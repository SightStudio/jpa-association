package orm;

import org.junit.jupiter.api.Test;
import orm.assosiation.RelationFields;
import persistence.sql.ddl.Order;

import static org.assertj.core.api.Assertions.assertThat;

class TableFieldClassifierTest {

    @Test
    void test() {
        // given
//        Order order = new Order();
//
//         when
//        TableFieldClassifier<Order> tableFieldClassifier = new TableFieldClassifier<>(order);
//        RelationFields<Order> relationFields = tableFieldClassifier.getRelationFields();
//
//         then
//        assertThat(relationFields.getRelationList()).hasSize(1);
    }
}
